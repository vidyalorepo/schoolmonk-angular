name: Build
on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - uses: docker://sonarsource/sonar-scanner-cli:latest
        env:
          GITHUB_TOKEN: ${{ secrets.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 }}
          SONAR_TOKEN: ${{ secrets.2f6112afee72f0baa582dc84c9e04209fe20f8a9 }}
          SONAR_HOST_URL: ${{ secrets.http://20.1.1.109:9000 }}
